"use client";
export default function TeacherCalendar() { return <div className="page-wrapper"><div className="page-header"><div><h1 className="page-title">Calendar</h1><p className="page-subtitle">Schedule classes, exams, and events</p></div><button className="btn btn-primary">+ Add Event</button></div><div className="content-grid"><div className="card"><div className="calendar-grid">{["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map(d => <div key={d} className="calendar-day-header">{d}</div>)}{Array.from({ length: 0 }).map((_, i) => <div key={`e-${i}`} className="calendar-day" style={{ opacity: 0.3 }}></div>)}{Array.from({ length: 28 }).map((_, i) => <div key={i + 1} className={`calendar-day ${i + 1 === 19 ? "today" : ""}`}>{i + 1}</div>)}</div></div><div className="card"><h3 className="card-title" style={{ marginBottom: "1rem" }}>Upcoming Schedule</h3>{[{ title: "Grade 11-A Math", time: "8:00-8:45", type: "class" }, { title: "Grade 12 Midterm", time: "9:00-11:00", type: "exam" }, { title: "Staff Meeting", time: "2:00 PM", type: "event" }].map((e, i) => (<div key={i} style={{ display: "flex", alignItems: "center", gap: "0.75rem", padding: "0.75rem", background: "var(--gray-50)", borderRadius: "var(--radius-md)", marginBottom: "0.5rem", borderLeft: `3px solid ${e.type === "exam" ? "var(--danger)" : e.type === "class" ? "var(--primary-500)" : "var(--warning)"}` }}><div><div style={{ fontWeight: 600, fontSize: "0.875rem" }}>{e.title}</div><div style={{ fontSize: "0.75rem", color: "var(--gray-500)" }}>{e.time}</div></div></div>))}</div></div></div>; }
