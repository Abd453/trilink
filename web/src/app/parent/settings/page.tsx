"use client";
import { useState } from "react";
export default function ParentSettings() { const [n, setN] = useState({ email: true, push: true, sms: false }); return <div className="page-wrapper"><div className="page-header"><h1 className="page-title">Settings</h1></div><div style={{ maxWidth: 640, display: "flex", flexDirection: "column", gap: "1rem" }}><div className="card"><h3 className="card-title" style={{ marginBottom: "1rem" }}>Notifications</h3>{Object.entries(n).map(([k, v]) => (<div key={k} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "0.75rem 0", borderBottom: "1px solid var(--gray-50)" }}><div><div style={{ fontWeight: 500, textTransform: "capitalize" }}>{k} Notifications</div><div style={{ fontSize: "0.75rem", color: "var(--gray-500)" }}>Receive alerts via {k}</div></div><button onClick={() => setN({ ...n, [k]: !v })} style={{ width: 44, height: 24, borderRadius: 12, background: v ? "var(--primary-500)" : "var(--gray-300)", position: "relative" }}><div style={{ width: 20, height: 20, borderRadius: "50%", background: "#fff", position: "absolute", top: 2, left: v ? 22 : 2, transition: "left 0.2s" }}></div></button></div>))}</div><div className="card"><h3 className="card-title" style={{ marginBottom: "1rem" }}>Language</h3><select style={{ padding: "0.625rem 1rem", borderRadius: "var(--radius-md)", border: "1.5px solid var(--gray-200)", background: "var(--gray-50)", fontSize: "0.9rem", width: "100%", fontFamily: "inherit" }}><option>English</option><option>Amharic (አማርኛ)</option></select></div></div></div>; }
